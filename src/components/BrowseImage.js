import React from 'react'

export default function BrowseImage(id) {
  return (
    <>
      <img
      src={ console.log(this.props.images[35].file_urls.thumbnail) }
      alt={this.props.element_texts[0].text} />
    </>
  )
}
